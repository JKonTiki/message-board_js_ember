
import { favoriteTrue } from 'message-board/helpers/favorite-true';
import { module, test } from 'qunit';

module('Unit | Helper | favorite true');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = favoriteTrue([42]);
  assert.ok(result);
});

