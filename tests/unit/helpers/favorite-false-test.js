
import { favoriteFalse } from 'message-board/helpers/favorite-false';
import { module, test } from 'qunit';

module('Unit | Helper | favorite false');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = favoriteFalse([42]);
  assert.ok(result);
});

